#!/bin/bash

# This script serves as a trampoline for running scripts which depend on
# libmakepkg with the libmakepkg within the build tree.

LIBRARY="/home/david/pacman/build/scripts"/libmakepkg exec "/usr/bin/bash" -$- "/home/david/pacman/build/scripts/pacman-db-upgrade" "$@"
